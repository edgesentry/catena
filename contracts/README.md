# contracts

Canonical interface contracts shared across pipeline, document, and apps.

## Baseline schemas

- `facts-schema/facts.schema.json`
- `manifest-schema/evaluation-manifest.schema.json`

Use `tools/scripts/validate-contracts.sh` to validate schema JSON integrity.
